services:
  brain:
    image: kitchencoder/second-brain:latest
    container_name: brain
    volumes:
      - ${BRAIN_HOST_PATH:-~/Documents/MyVault}:/brain
    env_file:
      - .env
    stdin_open: true
    tty: true
    restart: unless-stopped
